# راهنمای خیلی ساده pgAdmin (برای مبتدی‌ها)

## قدم 1: باز کردن pgAdmin

1. کلیک روی Start (پایین سمت چپ)
2. تایپ کن: `pgAdmin`
3. Enter بزن

اولین بار یه رمز ازت می‌خواد. یه رمز ساده بذار مثل: `admin`

---

## قدم 2: وصل شدن به PostgreSQL

در سمت چپ صفحه، می‌بینی:
```
Servers
  └─ PostgreSQL 17
```

1. کلیک روی `PostgreSQL 17`
2. یه پنجره باز میشه که می‌گه: "Password for user postgres"
3. رمزی که موقع نصب PostgreSQL تنظیم کردی رو بنویس (مثلاً: `postgres`)
4. تیک بزن روی "Save password"
5. کلیک OK

اگه موفق بود، یه علامت سبز می‌بینی ✅

---

## قدم 3: ساخت دیتابیس

### روش ساده (بدون کد):

1. در سمت چپ، پیدا کن: `Databases`
2. کلیک راست روی `Databases`
3. انتخاب کن: `Create` → `Database...`
4. یه پنجره باز میشه
5. در قسمت "Database"، بنویس: `gym_management`
6. کلیک روی `Save`

✅ دیتابیس ساخته شد!

---

## قدم 4: ساخت جداول

1. در سمت چپ، پیدا کن: `Databases` → `gym_management`
2. کلیک راست روی `gym_management`
3. انتخاب کن: `Query Tool`
4. یه صفحه سفید باز میشه

حالا:
1. باز کن فایل: `backend/create-tables.sql`
2. همه چیز رو انتخاب کن (Ctrl+A)
3. کپی کن (Ctrl+C)
4. برگرد به pgAdmin
5. Paste کن توی صفحه سفید (Ctrl+V)
6. کلیک روی دکمه ▶️ (Execute/Refresh) یا F5

باید پیام ببینی: `Query returned successfully`

✅ جداول ساخته شدن!

---

## قدم 5: چک کردن

در سمت چپ:
```
Databases
  └─ gym_management
      └─ Schemas
          └─ public
              └─ Tables
```

کلیک روی `Tables`. باید ببینی:
- ✅ attendance
- ✅ attendance_records  
- ✅ members
- ✅ transactions
- ✅ users

---

## قدم 6: دیدن داده‌ها

1. کلیک راست روی `members`
2. انتخاب: `View/Edit Data` → `All Rows`
3. باید 5 نفر رو ببینی (علی، سارا، محمد، فاطمه، حسین)

✅ داده‌های نمونه هم اضافه شدن!

---

## قدم 7: تنظیم Backend

باز کن فایل: `backend/.env`

چک کن این خط‌ها رو:
```env
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gym_management
DB_USER=postgres
DB_PASSWORD=postgres
```

اگه رمز متفاوتی داری، `DB_PASSWORD` رو عوض کن.

---

## قدم 8: تست

باز کن Terminal:
```bash
cd backend
npm run test-connection
```

باید ببینی:
```
✅ Database connected successfully!
✅ Tables found:
   - attendance
   - attendance_records
   - members
   - transactions
   - users
```

---

## قدم 9: اجرای Backend

```bash
npm run dev
```

باید ببینی:
```
🚀 Server is running on port 5000
✅ Connected to PostgreSQL database
```

---

## قدم 10: اجرای Frontend

Terminal جدید باز کن:
```bash
npm run dev
```

برو به: http://localhost:5173

---

## 🎉 تمام!

اگه صفحه باز شد و لیست اعضا رو دیدی، **موفق شدی!**

---

## ❓ سوالات متداول

### سوال: pgAdmin کجاست؟
**جواب:** Start Menu → تایپ کن: pgAdmin

### سوال: رمز PostgreSQL چیه؟
**جواب:** رمزی که موقع نصب تنظیم کردی. اگه یادت نیست، احتمالاً: `postgres`

### سوال: فایل create-tables.sql کجاست؟
**جواب:** در پوشه `backend` پروژه

### سوال: خطا میده "database already exists"
**جواب:** عیب نداره! دیتابیس از قبل وجود داره. برو قدم 4

### سوال: خطا میده "password authentication failed"
**جواب:** رمز اشتباهه. رمز صحیح رو در pgAdmin و backend/.env بذار

---

## 📞 کمک بیشتر

اگه گیر کردی:
1. اسکرین‌شات بگیر
2. خطا رو کپی کن
3. بفرست تا کمکت کنم

---

**موفق باشی!** 🚀
