# 🚀 راهنمای سریع - سیستم احراز هویت کامل

## مرحله 1: اجرای بک‌اند

```bash
cd backend
npm run dev
```

سرور روی `http://localhost:5000` اجرا می‌شود.

---

## مرحله 2: اجرای فرانت‌اند

```bash
npm run dev
```

فرانت‌اند روی `http://localhost:5173` اجرا می‌شود.

---

## مرحله 3: ورود به سیستم

مراجعه به `http://localhost:5173` و استفاده از یکی از حساب‌های زیر:

### 🔑 Super Admin (دسترسی کامل)
- **نام کاربری**: `superadmin`
- **رمز عبور**: `Admin@123`
- **دسترسی**: همه صفحات + لاگ فعالیت‌ها

### 👤 Admin (مدیر عادی)
- **نام کاربری**: `admin1`
- **رمز عبور**: `Admin123`
- **دسترسی**: همه صفحات (بدون لاگ فعالیت‌ها)

---

## ✅ قابلیت‌های پیاده‌سازی شده

### بک‌اند:
- ✅ Login/Register API با JWT
- ✅ Middleware احراز هویت
- ✅ Middleware بررسی نقش (role-based)
- ✅ محافظت از Activity Logs (فقط super_admin)

### فرانت‌اند:
- ✅ صفحه Login زیبا
- ✅ مدیریت Token در localStorage
- ✅ Protected Routes
- ✅ مخفی کردن منوها بر اساس نقش
- ✅ نمایش اطلاعات کاربر در Sidebar
- ✅ دکمه خروج

---

## 📖 مستندات کامل

- **بک‌اند**: `backend/AUTH_SETUP_GUIDE.md`
- **فرانت‌اند**: `PHASE2_COMPLETE.md`
- **مرحله 1**: `PHASE1_COMPLETE.md`

---

## 🧪 تست سریع

### تست Super Admin:
1. ورود با `superadmin` / `Admin@123`
2. بررسی نمایش "لاگ فعالیت‌ها" در منو ✅
3. کلیک روی "لاگ فعالیت‌ها" → دسترسی موفق ✅

### تست Admin عادی:
1. خروج از حساب super_admin
2. ورود با `admin1` / `Admin123`
3. بررسی عدم نمایش "لاگ فعالیت‌ها" ✅
4. تلاش برای دسترسی مستقیم به `/activity-logs` → Redirect ✅

---

## 🎉 همه چیز آماده است!

سیستم احراز هویت کامل و آماده استفاده است. لذت ببرید! 🚀
