# 📊 راهنمای نصب سیستم لاگ فعالیت‌ها

## مرحله 1: آپدیت دیتابیس

1. **pgAdmin را باز کنید**
2. به دیتابیس `gym_management` متصل شوید
3. فایل `backend/scripts/add-activity-logs-table.sql` را باز کنید
4. محتوای آن را کپی کرده و در Query Tool اجرا کنید
5. پیام موفقیت را مشاهده کنید

## مرحله 2: تست Backend

سرور backend به صورت خودکار ری‌استارت شده و آماده است.

### تست API:

```bash
# تست دریافت لاگ‌ها
curl http://localhost:5000/api/activity-logs

# تست دریافت آمار
curl http://localhost:5000/api/activity-logs/stats
```

## مرحله 3: مشاهده در Frontend

1. به آدرس `http://localhost:5173/activity-logs` بروید
2. لاگ‌های نمونه را مشاهده کنید
3. فیلترها را امتحان کنید

## ویژگی‌های سیستم لاگ:

### ✅ ثبت خودکار فعالیت‌ها:
- ✅ ایجاد/ویرایش/حذف اعضا
- ✅ ایجاد/ویرایش/حذف تراکنش‌ها
- ✅ ثبت/حذف حضور و غیاب
- ✅ سوالات از دستیار هوش مصنوعی

### 📊 آمار و گزارش:
- فعالیت 24 ساعت اخیر
- بیشترین عملیات انجام شده
- بیشترین بخش استفاده شده
- فعال‌ترین کاربر

### 🔍 فیلترهای پیشرفته:
- فیلتر بر اساس نوع عملیات
- فیلتر بر اساس بخش
- فیلتر بر اساس کاربر
- فیلتر بر اساس بازه زمانی

### 📄 صفحه‌بندی:
- نمایش 50 لاگ در هر صفحه
- امکان جابجایی بین صفحات

## نکات مهم:

1. **لاگ‌ها فقط برای عملیات موفق ثبت می‌شوند** (status code 200-299)
2. **IP Address و User Agent ذخیره می‌شود** برای امنیت بیشتر
3. **عملیات GET ثبت نمی‌شوند** تا حجم لاگ‌ها کم بماند
4. **لاگ‌های قدیمی قابل حذف هستند** (پیش‌فرض: بیش از 90 روز)

## مثال استفاده:

وقتی یک عضو جدید اضافه می‌کنید:
```
✅ لاگ ثبت می‌شود:
- عملیات: ایجاد
- بخش: عضو
- توضیحات: عضو جدید ثبت شد: علی احمدی
- زمان: 1403/08/19 - 23:42:15
- کاربر: مهمان
```

## پشتیبانی:

اگر مشکلی داشتید:
1. لاگ‌های سرور را چک کنید
2. مطمئن شوید جدول `activity_logs` ساخته شده
3. مطمئن شوید سرور backend در حال اجرا است

---

**نکته:** این سیستم به صورت خودکار کار می‌کند و نیازی به تنظیمات اضافی ندارد! 🎉
