# تست سیستم لاگ فعالیت‌ها

## ✅ لاگ‌های جدید اضافه شده:

### 1️⃣ مدیریت کاربران (Users)
```
✅ ایجاد کاربر جدید
✅ ویرایش اطلاعات کاربر
✅ حذف کاربر
✅ تغییر رمز عبور
✅ فعال/غیرفعال کردن کاربر
```

### 2️⃣ احراز هویت (Authentication)
```
✅ ورود موفق به سیستم
✅ ورود ناموفق (رمز اشتباه)
✅ خروج از سیستم
```

---

## 🧪 نحوه تست:

### تست 1: ورود به سیستم
```bash
# ورود موفق
POST /api/auth/login
{
  "username": "admin",
  "password": "correct_password"
}
# انتظار: لاگ "ورود" ثبت شود

# ورود ناموفق
POST /api/auth/login
{
  "username": "admin",
  "password": "wrong_password"
}
# انتظار: لاگ "ورود ناموفق" ثبت شود
```

### تست 2: خروج از سیستم
```bash
POST /api/auth/logout
Headers: Authorization: Bearer YOUR_TOKEN
# انتظار: لاگ "خروج" ثبت شود
```

### تست 3: ایجاد کاربر جدید
```bash
POST /api/users
Headers: Authorization: Bearer SUPER_ADMIN_TOKEN
{
  "username": "test_user",
  "email": "test@example.com",
  "password": "Test@123",
  "role": "admin"
}
# انتظار: لاگ "ایجاد کاربر" ثبت شود
```

### تست 4: ویرایش کاربر
```bash
PUT /api/users/1000
Headers: Authorization: Bearer SUPER_ADMIN_TOKEN
{
  "username": "updated_user",
  "email": "updated@example.com"
}
# انتظار: لاگ "ویرایش کاربر" ثبت شود
```

### تست 5: تغییر رمز عبور
```bash
PUT /api/users/1000/change-password
Headers: Authorization: Bearer TOKEN
{
  "currentPassword": "old_password",
  "newPassword": "new_password"
}
# انتظار: لاگ "تغییر رمز" ثبت شود
```

### تست 6: حذف کاربر
```bash
DELETE /api/users/1000
Headers: Authorization: Bearer SUPER_ADMIN_TOKEN
# انتظار: لاگ "حذف کاربر" ثبت شود
```

### تست 7: فعال/غیرفعال کردن کاربر
```bash
PUT /api/users/1000/toggle-status
Headers: Authorization: Bearer SUPER_ADMIN_TOKEN
# انتظار: لاگ "تغییر وضعیت" ثبت شود
```

---

## 📊 مشاهده لاگ‌ها:

### در صفحه Activity Logs:
1. برو به `/activity-logs`
2. فیلتر کن بر اساس:
   - **عملیات**: ورود، خروج، ایجاد، ویرایش، حذف، ...
   - **بخش**: کاربر، احراز هویت، عضو، ...
   - **کاربر**: نام کاربر خاص
   - **تاریخ**: بازه زمانی

### مثال لاگ‌های نمایش داده شده:
```
✅ ادمین "محمد" وارد سیستم شد
✅ ادمین "شیدا" کاربر جدید ایجاد شد: test_user (test@example.com) - نقش: مدیر
✅ ادمین "محمد" اطلاعات کاربر ویرایش شد: updated_user
✅ ادمین "شیدا" رمز عبور خود را تغییر داد
✅ ادمین "محمد" وضعیت کاربر (ID: 1000) به غیرفعال تغییر کرد
✅ ادمین "شیدا" کاربر حذف شد (ID: 1000)
✅ ادمین "محمد" از سیستم خارج شد
```

---

## 🎯 نکات مهم:

1. **IP Address**: آدرس IP کاربر ثبت میشه
2. **User Agent**: اطلاعات مرورگر ثبت میشه
3. **زمان دقیق**: تاریخ و ساعت دقیق ثبت میشه
4. **توضیحات کامل**: جزئیات کامل عملیات ثبت میشه

---

## ✅ چک لیست تکمیل:

- [x] لاگ ورود موفق
- [x] لاگ ورود ناموفق
- [x] لاگ خروج
- [x] لاگ ایجاد کاربر
- [x] لاگ ویرایش کاربر
- [x] لاگ حذف کاربر
- [x] لاگ تغییر رمز عبور
- [x] لاگ تغییر وضعیت کاربر
- [x] آپدیت فیلترهای صفحه
- [x] آپدیت رنگ‌بندی عملیات
- [x] آپدیت آیکون‌های بخش‌ها
